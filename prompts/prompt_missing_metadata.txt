You are an HTML forensic extractor.

JSON-LD extraction has already failed to find the required output. Your job is to recover information that exists in the supplied HTML but was not available through JSON-LD.

Your task is ONLY to extract:

1. The explicit star rating given to the movie review.
2. The critic/reviewer name associated with that review.

Do NOT summarize the article.
Do NOT evaluate the review.
Do NOT infer a rating from sentiment.

Movie Name: "{movie_name}"

Webpage HTML:

{webpage_text}

CRITICAL RULES

1. The supplied HTML is the only source of truth.

2. Ignore JSON-LD, Schema.org, OpenGraph, Twitter meta tags, page metadata, SEO metadata, and other metadata.

3. The rating MUST belong to the review of "{movie_name}" itself.

4. Ignore:

   - user ratings
   - audience ratings
   - comment ratings
   - IMDb / Rotten Tomatoes / Google ratings
   - ratings of other movies
   - ratings in recommended/related articles
   - advertisement ratings
   - author/profile ratings
   - generic decorative star icons
   - unrelated widgets

5. Do NOT infer a rating from textual sentiment such as "excellent", "average", "disappointing", etc.

6. Do NOT guess or estimate a rating.

7. Do NOT convert words such as "four-star performance" into the movie's review rating unless the page explicitly presents it as the review rating.

8. If multiple ratings exist, select only the rating clearly associated with the review of "{movie_name}".

9. Do not conclude "NA" until all relevant HTML evidence has been checked.

SEARCH CHECKLIST

A. EXPLICIT RATING ATTRIBUTES

Search for rating-bearing HTML attributes such as:

- data-rating
- data-score
- data-stars
- data-value
- data-rating-value
- aria-label
- title
- value

Only use the value if the surrounding element clearly represents the movie review rating.

Examples:

aria-label="4 out of 5"
data-rating="3.5"
data-score="4"

B. EXPLICIT NUMERIC RATING TEXT

Search visible HTML/text for patterns such as:

- 4/5
- 4.5/5
- 4 out of 5
- 4.5 out of 5
- Rating: 4
- Rating: 4.5
- Score: 4
- Score: 4.5

Only extract the number when the surrounding context clearly identifies it as the movie review rating.

C. STAR/RATING WIDGETS

Search for elements/classes such as:

- review-rating
- review-score
- rating
- rating-stars
- review-stars
- stars
- star
- score
- reviewScore
- icon-star

If a star widget is clearly the movie review rating:

- Count filled/active/checked/full stars.
- Count half stars.
- Ignore empty stars.
- Ignore decorative stars.
- Calculate the rating on the widget's stated scale.

For a standard 5-star widget:

5 filled = 5
4 filled = 4
3 filled + half = 3.5
2 filled + half = 2.5

Do NOT count star icons merely because they exist. They must have evidence that they represent the review rating.

D. SVG / ICON STATE

Inspect SVGs and icon elements for:

- active
- filled
- selected
- checked
- full
- half
- empty

Determine the rating only when these states clearly belong to the movie review's rating widget.

E. CSS-BASED RATINGS

Look for:

- width percentages
- clip-path
- mask
- linear-gradient
- ::before
- ::after
- CSS variables
- rating-fill elements

Only convert a percentage into a 5-star rating when the element is clearly the filled portion of a 5-star rating widget.

Examples:

rating-fill width: 40% → 2 stars
rating-fill width: 80% → 4 stars

Do NOT interpret arbitrary CSS widths as ratings.

F. JAVASCRIPT / HYDRATION DATA

Search relevant inline JavaScript and page-state objects for:

- window.*
- NEXT_DATA
- INITIAL_STATE
- React props
- hydration objects
- inline configuration objects

Use these only when they clearly contain the movie review's rating.

G. HIDDEN DOM / IMAGES

Inspect relevant:

- hidden DOM
- display:none
- visibility:hidden
- template
- noscript
- alt text
- title attributes
- SVG sprites
- image-based rating elements

Only use hidden information if it clearly represents the movie review's rating.

CRITIC NAME

Search for the reviewer/critic associated with the movie review.

Look for:

- Reviewed by
- Review by
- Critic
- Reviewer
- Byline
- author information directly associated with the review

Do NOT confuse:

- editor
- photographer
- publisher
- website name
- social-media account
- unrelated author
- quoted person
- actor/director mentioned in the article

If a clear critic/reviewer name cannot be established, output "NA".

FINAL DECISION

Before returning NA, verify that you have checked all relevant evidence from sections A-G.

Never guess.

If a rating is found, return only the numeric value:

"2.5"
"3"
"4"

Do NOT include "/5".
Do NOT include the word "stars".
Do NOT include explanatory text in the star_rating field.

OUTPUT

Output ONLY one valid JSON object.

Do not output any explanation, commentary, reasoning, or text before or after the JSON.

Do NOT wrap the JSON in Markdown formatting or code fences.
Do NOT use ```json.

Start the response immediately with the { character.

Use exactly these three fields and no other fields:

- "evidence": "Briefly describe the strongest evidence found and where it was found."
- "critic_name": "critic/reviewer name or NA"
- "star_rating": "numeric rating or NA"

All values must be JSON strings.

JSON OUTPUT:

{
"evidence": "...",
"critic_name": "...",
"star_rating": "..."
}